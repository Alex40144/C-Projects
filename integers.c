#include <stdio.h> 
 
int main(void) {
    int a = 10; //assign a variable
    int b = 15; //assign variab,e
    float c = 12.6; //float
    printf("%d %d %f", a, b, c); //print to console with correct specifiers 
    return 0;
}